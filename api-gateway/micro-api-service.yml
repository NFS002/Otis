apiVersion: v1
kind: Service
metadata:
  name: micro-api
  namespace: default
  labels:
    name: micro-api
    micro: service
spec:
  ports:
    - name: http
      port: 80
      targetPort: "api-port"
  selector:
    name: micro-api
    micro: service
  type: LoadBalancer