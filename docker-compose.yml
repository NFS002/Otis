# docker-compose.yml
version: '3.5'

services:

  api_gateway:
    image: microhq/micro:latest
    restart: always
    command: api --handler=api
    ports:
      - 8080:8080
    networks:
      - api-gateway-tier

  api_merchant:
    restart: always
    container_name: "go.micro.api.merchant"
    build:
      context: ./api/merchant/
      dockerfile: Dockerfile
      args:
        - gitlab_user=otis_cj
        - gitlab_token=1fruBCF2_5aLxaF3NBcf
    depends_on:
      - api_gateway
    ports:
      - 50051:50051
    networks:
      - api-gateway-tier
      - api-merchant-tier

  service_merchant:
    restart: always
    container_name: "go.micro.service.merchant"
    build: ./service/merchant/
    ports:
      - 50052:50052
    links:
      - merchant-datastore
    networks:
      - api-gateway-tier
      - api-merchant-tier
      - service-merchant-tier
    depends_on:
      - merchant-datastore
    environment:
      DB_HOST: "mongodb://merchant-datastore:27017"

  merchant-datastore:
    image: mongo:latest
    container_name: "merchant-datastore"
    environment:
      - MONGO_DATA_DIR=/data/db
      - MONG_LOG_DIR=/dev/null
    volumes:
      - ./data/db:/data/db
    networks:
      - service-merchant-tier
    ports:
      - 27017:27017
    command: mongod --logpath=/dev/null


networks:
  api-gateway-tier:
    name: api-gateway-tier
  api-merchant-tier:
    name: api-merchant-tier
  service-merchant-tier:
    name: service-merchant-tier